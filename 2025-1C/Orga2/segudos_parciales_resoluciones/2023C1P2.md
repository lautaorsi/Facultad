5 tareas que pueden enviar resultados a una 6ta
se almacena el res en eax, cede su tiempo a la 6ta

a. Cada una debe tener su correspondiente directorio y tabla de paginacion donde almacena sus datos, codigo y pilas, al pasar el parametro mediante un registro no deberia -en principio- necesitar un sector de memoria compartida espeificamente para esta nueva feature

b. la syscall tiene que poder ser llamada x lvl 3, IDTENTRY3(99) con 99 un numero arbitrario

//voy a asumir que tenemos un task_6_id que representa el indice de la tss en tss table

_isr99:

    pushad

    push eax

    call modificar_eax_tarea_6 ;devolverá el selector de segmento de la tarea 6 para poder ejecutarla 

    jmp eax

    popad
    iret


uint16_t modificar_eax_tarea_6(uint32_t eax){
    
    uint16_t tss_selector = sched_tasks[task_6_id].selector; //Ahora que tenemos el selector, tenemos que ir a buscar el contexto a la gdt

    tss_t* tss_task = gdt[tss_selector >> 3].base //obtenemos el tss de la tarea

    tss_task.eax = eax //guardamos el valor que nos pasaron en el registro eax de la tarea 6

    return tss_selector //retornamos el selector para poder hacer el salto a la tarea
}


